name: zero_alert

version: 0.0.1

description: ZeroAlert is an alert with pre-defined styles for display famous messages to normal user, like success, error ...etc, with the ability to customization.

repository: https://github.com/PresKhaled/zero-alert

issue_tracker: https://github.com/PresKhaled/zero-alert/issues

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"
 
dependencies:
  flutter:
    sdk: flutter
